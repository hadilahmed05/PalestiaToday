trigger:
  branches:
    include:
      - main

pool:
  name: Default

jobs:
- job: SecurityScanForFrontendImage
  displayName: 'Scan container image for vulnerabilities in fontend image'
  steps:
  - task: CmdLine@2
    inputs:
      script: |
        trivy image --format table \
                    --output trivy-image-report.html \
                    hadil0505/palestia:palestiaFrontend 